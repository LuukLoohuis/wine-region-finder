# Mapbox token — mag public zijn, beperk wel tot jouw domein op mapbox.com/account/tokens
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# Claude API-sleutel wordt NIET hier opgeslagen.
# Vul hem in via het ⚙ icoon in de app — hij wordt lokaal bewaard in localStorage.
